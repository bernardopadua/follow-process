import processes from '../data/processes_fake.json'

class FollowProcessAPI {
    getProcesses(){

        return new Promise(
            (res, err) => {
                
            }
        );
    }
}